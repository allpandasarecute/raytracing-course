#pragma once

#include "glm/glm.hpp"
#include <string>
#include <utility>
#include <vector>

using glm::vec3;
using std::string;
using std::vector;

typedef unsigned char byte;
typedef unsigned int uint;
typedef std::pair<uint, uint> ind;
